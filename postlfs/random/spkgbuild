# description   : No description available
# depends       : 

name=random number generation
version=
release=1
source=""

options="!checksum"

build() {
    cd $name-$version
    
    make DESTDIR=$PKG install-random
}
