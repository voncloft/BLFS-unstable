# description   : The librsvg package contains a library and tools used to manipulate, convert and view Scalable Vector Graphic (SVG) images
# depends       : cairo cargoc gdkpixbuf pango glib vala

name=librsvg
version=2.60.0    
release=1
source="https://download.gnome.org/sources/librsvg/2.60/librsvg-$version.tar.xz"

options="!checksum"

build() {
    cd $name-$version
    
    sed -e "/OUTDIR/s|,| / 'librsvg-$version', '--no-namespace-dir',|" \
    -e '/output/s|Rsvg-2.0|librsvg-$version|'                      \
    -i doc/meson.build
    mkdir build 
    cd    build 
    meson setup --prefix=/usr --buildtype=release .. 
    ninja
    DESTDIR=$PKG ninja test
    DESTDIR=$PKG ninja install
}
