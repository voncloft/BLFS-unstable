# description   : The GC package contains the Boehm-Demers-Weiser conservative garbage collector, which can be used as a garbage collecting replacement for the C malloc function or C++ new operator
# depends       : 

name=gc
version=8.2.8    
release=1
source="https://github.com/ivmai/bdwgc/releases/download/v$version/gc-$version.tar.gz"

options="!checksum"

build() {
    cd $name-$version
    
    ./configure --prefix=/usr      \
    --enable-cplusplus \
    --disable-static   \
    --docdir=/usr/share/doc/gc-$version 
    make
    make DESTDIR=$PKG install 
    install -v -m644 doc/gc.man /usr/share/man/man3/gc_malloc.3
}
